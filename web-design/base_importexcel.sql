create table data(
     societe varchar(100),
     n_employe integer not null,
     ville varchar(100),
     pays varchar(100),
     Date_commande date,
     n_commande integer not null,
     nom_du_produit varchar(100),
     prix_total double precision
);


insert into data values
('','','','','','','','');